// PhotoSlideshow is currently UNUSED and requires 'swiper' package
// Skipping tests until component is actually used in the application

describe.skip('PhotoSlideshow', () => {
  it('is marked as unused - requires swiper package', () => {
    // This component is not currently used in the application
    // and would require installing the 'swiper' package dependency
    expect(true).toBe(true);
  });
});
